# `go.mod`

Defines the backend Go module as `addispay-integration`, selects the Go language version, and declares direct dependencies.

Run `go mod tidy` when dependencies change, and use `go test ./...` to compile and test all packages.
