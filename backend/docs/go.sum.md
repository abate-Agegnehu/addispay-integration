# `go.sum`

Contains cryptographic checksums for the backend's Go modules. Go uses this file to verify dependency contents during builds.

It is generated and updated by Go tooling rather than edited manually.
