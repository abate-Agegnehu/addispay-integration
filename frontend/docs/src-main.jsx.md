# `src/main.jsx`

React bootstrap entry point.

## How It Works

Creates a React DOM root from the `root` element in `index.html`, enables `React.StrictMode`, and renders the top-level `App` component.
