# `package.json`

Defines the frontend package metadata, scripts, and dependencies.

## Scripts

- `npm run dev`: starts Vite for local development.
- `npm run build`: creates the production bundle.
- `npm run preview`: serves the production bundle locally.

React, React Router, Axios, Vite, and the React Vite plugin are declared here.
