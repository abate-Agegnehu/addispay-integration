# `index.html`

Vite's browser entry document.

## How It Works

It defines the HTML metadata, provides the `<div id="root">` mount point, and loads `/src/main.jsx` as a module. React renders the application inside that root element.
